#include <stdio.h>
#include <stdlib.h>
#include "Questions.h"

void string_copy(const char source[], char destination[], int n){
int i =0;
	 for(i = 0; i<n-1; i++){ // iterates through size of source -1
	   destination[i] = source[i]; // adding source to destination of same array
	 }
	 destination[n-1] = '\0'; // adds null component to destination array




	
    
}
